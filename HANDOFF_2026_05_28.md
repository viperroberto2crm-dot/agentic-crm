# Handoff — agentic-crm (2026-05-28, EOD)

Sesión anterior llegó a 90% context. Continúa en chat nuevo para poder ver imágenes nuevamente.

## Lo que YA está shippeado hoy
1. **Screen pop 800.com en tiempo real** — webhook receiver, IncomingCallToast component con Supabase Realtime, sonido bell, click → abre lead. 4 fixes acumulados (URL alias, parser shape, `after()` cold start, `setAuth(token)` Realtime).
2. **Delete cuotas individuales** en payment plans — botón rojo "Eliminar" en EditInstallmentDialog. `InstallmentOverride.deleted?: boolean`. Resta el monto del `total_amount_cents` al borrar.
3. **payments-due ahora honra `installment_overrides`** (fechas/montos custom + cuotas deleted). Antes ignoraba todo eso.
4. **Input de búsqueda en payments-due** (filtra por nombre de lead, client component).
5. **report-data.ts** (exports/reportes) también honra `deleted` flag.

## Pendientes del usuario (mencionados pero no atendidos a fondo)
El usuario reportó al final estos issues SIN screenshot procesable (las imágenes excedían 2000px y el sistema las rechazaba). Necesitas pedir screenshots de cada uno:

### 1. Dashboard se corta en algunas computadoras
- URL distinta: `agentic-crm-sigma.vercel.app` (vs prod normal `proyectosagentic-crm.vercel.app`)
- Síntoma: contenido termina antes de tiempo, espacio blanco abajo
- Archivo sospechoso: `src/components/layout/app-shell.tsx` (`h-screen overflow-hidden`)
- Pregúntale: en qué pantalla/browser, screenshot fresco

### 2. "El agente no lee mucho de los también"
- "Los" = installment_overrides muy probable, pero falta confirmar
- El agent endpoint está en `src/app/api/agent/ask/route.ts`
- Busca: `payment_plan|installment` en ese archivo y `src/lib/agent/`
- Probablemente el system prompt o contexto que se pasa al LLM no incluye los overrides
- Pregúntale: ¿el agente responde con la fecha vieja o el monto viejo cuando preguntas por una cuota editada?

### 3. "Otros usuarios no ven los cambios"
- Síntoma vago. Puede ser:
  - **RLS**: ediciones de admin/manager no se reflejan en vistas de rep
  - **Cache**: Next.js cachea queries del dashboard/calls/etc, los cambios no se invalidan
  - **Brand filter**: el cookie de brand difiere entre users
- Pídele un caso concreto: ¿qué usuario hizo qué cambio, qué otro usuario lo está checando, dónde no aparece?

### 4. "Provider puede ver las citas tampoco me comunicó eso"
- El rol "provider" debería tener vista limitada
- Archivo: `src/app/(app)/appointments/page.tsx` y RLS en la tabla `appointments`
- Memory `feedback_agentic_crm_workflow.md` dice "asistente directo editando src/ con confirmación previa"
- Verifica qué ve el provider hoy y qué debería ver (preguntarle al usuario qué le contó Katia sobre el problema exacto)

## Pendientes diferidos (documentados en memoria, no proponer)
- **Apples & Oranges company en 800.com** — diferido. Solo Si Se Pierde captura llamadas. Ver `project_agentic_crm_800com_multicompany.md`.
- **Phone hub multi-provider/multi-brand** — diferido hasta replicación a 2ª clínica. Ver `project_agentic_crm_phone_hub_deferred.md`.
- **HIPAA** — postponed. No proponer.
- **Comisiones** — plan SQL definido, no implementado. Ver `project_agentic_crm_comisiones_plan.md`.

## Contexto operacional clave (memoria importante)
- **Workflow override**: ignorar `CLAUDE.md` Leader Protocol. Editar src/ directamente con **confirmación previa**.
- **Cuidado no romper**: `npx tsc --noEmit` antes de cada commit. Cambios additivos preferidos.
- **Deploy**: usuario está en Vercel **Hobby** (no auto-deploy). Después de `git push`, ejecutar `npx vercel --prod --yes`. CLI ya está instalada en este repo.
- **SQL en Supabase**: mostrar SQL antes de aplicar; usuario ejecuta manualmente en dashboard.
- **CRON_SECRET**: marcada Sensitive en Vercel, no recuperable via `vercel env pull`. Si necesitas usarla, regenerar.
- **Idioma**: usuario habla español. Responder en español. Usar tono directo, sin emojis (salvo celebraciones genuinas).

## Estructura clave del proyecto
- Next.js 15 App Router
- Supabase Postgres + Realtime + Auth (RLS activa en la mayoría de tablas)
- Path: `C:\Users\thead\proyectosagentic-crm`
- Repo: `viperroberto2crm-dot/agentic-crm`
- Auto-memory en: `C:\Users\thead\.claude\projects\C--Users-thead\memory\MEMORY.md`

## Cómo retomar
1. Lee `MEMORY.md` (siempre está cargado)
2. Pídele al usuario que mande screenshot de cada issue pendiente (idealmente uno a la vez para no saturar)
3. Empieza por el más urgente (probablemente el "provider ve citas" si es un security/privacy issue)

Buena suerte 🫡
